{{- if .Values.consumer.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "msa.fullname" . }}-consumer
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "msa.fullname" . }}-consumer
  ports:
    - port: {{ .Values.consumer.port }}
      targetPort: {{ .Values.consumer.port }}
      protocol: TCP
{{- end }}
